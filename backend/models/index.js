// Simple models index that exports all models
const Users = require('./users');
const Lands = require('./lands');
const Orders = require('./orders');
const OrderItems = require('./orderItems');

module.exports = {
  Users,
  Lands,
  Orders,
  OrderItems,
};
